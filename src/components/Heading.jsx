import React from "react";

const Heading = ({ text }) => {
  return (
    <h1 className="font-bold text-xl capitalize text-center py-4">
      new lower prices on must-haves
    </h1>
  );
};

export default Heading;
