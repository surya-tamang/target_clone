import React, { useState } from "react";

const Header = () => {
  return (
    <header className="sticky top-0">
      {/* Top navigation */}
      {/* main navigation  */}
    </header>
  );
};

export default Header;
